// Re-export from i18n/formatting for backward compatibility
// This file is kept for existing imports, but new code should use i18n/formatting directly
export {
  formatDate,
  formatRelativeTime,
  formatNumber,
  formatCurrency,
  formatDateTime,
  formatTime,
  getDateLocale,
  getIntlLocale,
} from "@/lib/i18n/formatting";

